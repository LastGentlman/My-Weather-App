import {LoadingContainer, Loader} from "./loading.syle";

export default function Loading() {
    return (
        <LoadingContainer>
            <Loader>
                <div></div>
            </Loader>
        </LoadingContainer>
    )
}